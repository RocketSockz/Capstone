/**
 * Helper function which specifies whether a string contains any sub-string in an array.
 * @param inputString string used for comparison.
 * @param includesSubStrings array of strings we want to see if are included in inputString.
 * @returns a boolean value - true if any substring of the array is found in inputString; false if not.
 */
export declare function includesAny(inputString: string, includesSubStrings: string[]): boolean;
