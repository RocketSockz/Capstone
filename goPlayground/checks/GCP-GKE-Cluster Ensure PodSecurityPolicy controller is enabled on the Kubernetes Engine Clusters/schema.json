{
  "recommended_action": "To enable Pod Security Policy for an existing cluster, run the following command:\ngcloud beta container clusters update [CLUSTER_NAME] --zone [COMPUTE_ZONE] --\nenable-pod-security-policy",
  "rationale": "The PodSecurityPolicy defines a set of conditions that Pods must meet to be accepted by the cluster; when a request to create or update a Pod doesnot meet the conditions in the PodSecurityPolicy, that request is rejected and an error is returned. The PodSecurityPolicy admission controller validates requests against available PodSecurityPolicies. PodSecurityPolicies specify a list of restrictions, requirements, and defaults for Pods created under the policy.",
  "source": "cis gcp 1.0.0",
  "properties": {
    "document": {
      "properties": {
        "podSecurityPolicyConfig": {
          "properties": {
            "enabled": {
              "errorMessage": "Kubernetes cluster does not have pod security policy enabled.",
              "const": true
            }
          }
        }
      },
      "errorMessage": "Kubernetes cluster's pod security policy could not be found.",
      "required": ["podSecurityPolicyConfig"]
    }
  },
  "schemaId": "c88e3642-86c1-031b-d158-92445b266353"
}
